"use client";

export function DemoBanner() {
  return (
    <div className="demo-banner" role="status">
      <strong>DEMO DATA</strong>
      <span>
        Floor is synthetic. Batches / What-if use the anonymized Excel mass log — not live PLC.
        Ask via Claude Desktop + MCP (see docs/MCP-CLAUDE.md).
      </span>
    </div>
  );
}
